mpfrtests.sh
  Script to perform tests of MPFR tarballs with various options.

mpfrtests.data
  Example of mpfrtests.data file for the mpfrtests.sh script.
  Users who want to use mpfrtests.sh should make their own
  mpfrtests.data file (because the list of machines and paths
  will differ).

release-3.1.0-p8
  Example of mpfrtests.sh output on various machines (obtained
  with GNU MPFR 3.1.0-p4 and 3.1.0-p8 tarballs).

release-3.1.2-p11
  Example of mpfrtests.sh output on various machines (obtained
  with GNU MPFR 3.1.2 and 3.1.2-p* tarballs).

vfy-data
  Check the format of the mpfrtests data file.
